box:
  id: akinobufujii/androidbuildpack
  tag: latest
  username: $USERNAME
  password: $PASSWORD

build:
   steps:
    - script:
      name: build start
      code: echo "build start"
    - script:
      name: clone source
      code: git clone https://github.com/googlesamples/android-AppShortcuts.git
    - script:
        name: build
        code: cd android-AppShortcuts | ./gradlew build
    - script:
        name: done
        code: echo "done"