box:
  id: akinobufujii/androidbuildpack
  tag: latest
  username: $USERNAME
  password: $PASSWORD

build:
   steps:
    - script:
      name: build start
      code: echo "build start"
    - script:
      name: clone source and build
      code: git clone https://github.com/googlesamples/android-AppShortcuts.git
      code: cd android-AppShortcuts
      code: ls
      #code: ./gradlew build
    - script:
        name: build end
        code: echo "build end"